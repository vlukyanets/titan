[project]
name = "spike-crsqlite"
version = "0.0.0"
description = "Throwaway spike: SQLite + cr-sqlite + sqlite-vec, 3 nodes"
requires-python = ">=3.11,<3.12"
dependencies = [
  "httpx>=0.27",
  "numpy>=2.0",
]

[tool.uv]
package = false
