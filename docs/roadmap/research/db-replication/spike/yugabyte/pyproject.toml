[project]
name = "spike-yugabyte"
version = "0.0.0"
description = "Throwaway ADR 0006 spike harness for YugabyteDB (YSQL)"
requires-python = ">=3.11,<3.12"
dependencies = [
    "alembic>=1.13",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
    "numpy>=1.26",
]

[tool.uv]
package = false
