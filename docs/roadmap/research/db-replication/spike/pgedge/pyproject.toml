[project]
name = "spike-pgedge"
version = "0.0.0"
requires-python = ">=3.11,<3.12"
dependencies = [
  "psycopg[binary]>=3.2",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
  "pgvector>=0.3",
  "numpy>=2.0",
]

[tool.uv]
package = false
